# 📖 ΤΟ ΒΙΒΛΙΟ ΤΟΥ FULLSTACK ENGINEER
## 📘 ΚΕΦΑΛΑΙΟ 1: Από το Χάος του XML στο Καθαρό JSON

```text
===================================================================================
                                 ΣΥΝΟΨΗ ΚΕΦΑΛΑΙΟΥ
   • 1.1 Η Ανάθεση του Μάνατζερ & Το Πρώτο Σοκ με τα 16MB
   • 1.2 Γιατί αποτυγχάνει η πρώτη διαίσθηση του 2D Πίνακα
   • 1.3 Η Εξερεύνηση στο Τερματικό με το curl
   • 1.4 Το Πρώτο Πείραμα: xml2js και η μάχη με το [Object]
   • 1.5 Ο Χρυσός Κανόνας Πλοήγησης: Τελείες (.) vs Αγκύλες ([])
   • 1.6 Το "Σκάψιμο" των Καναλιών (Channel Transformation)
   • 1.7 Το "Σκάψιμο" των Προγραμμάτων (Programme Transformation)
   • 1.8 Γιατί 2 Ξεχωριστά Loops και όχι 1 ενιαίο;
   • 1.9 Το Πρόβλημα του Χρόνου, το Invalid Date & Το Ψηφιακό Ψαλίδι (.slice)
   • 1.10 Καθαρισμός Domain Καναλιού & Μοναδικό ID Εκπομπής
   • 1.11 Ο Τελικός Κώδικας του parser_2.js (Έτοιμο Module)
   • 1.12 Επαγγελματικό Στήσιμο Git & Το Σωστό .gitignore
===================================================================================
```

---

### 1.1 Η Ανάθεση του Μάνατζερ & Το Πρώτο Σοκ

Φαντάσου την πρώτη μέρα στη δουλειά: Ο μάνατζερ σου δίνει ένα link:  
`https://ext.greektv.app/epg/epg.xml`  
Και σου ζητάει: *"Φτιάξε ένα web app που να δείχνει το πρόγραμμα της τηλεόρασης σαν ημερολόγιο."*

---

### 1.2 Γιατί Αποτυγχάνει η Πρώτη Διαίσθηση του 2D Πίνακα

Όταν βλέπουμε ένα ημερολόγιο με γραμμές και στήλες, ο εγκέφαλος λέει αυθόρμητα:  
*«Α, ένας πίνακας Excel / 2D Matrix στη JavaScript! Γραμμές οι ώρες (00:00, 01:00, 02:00...), στήλες τα κανάλια.»*  

#### 🛑 Γιατί καταρρέει αυτή η ιδέα στην πράξη;
Επειδή οι εκπομπές δεν έχουν σταθερή διάρκεια! 
* Μια εκπομπή κρατάει 45 λεπτά (10:15 - 11:00).
* Μια ταινία κρατάει 1 ώρα και 20 λεπτά (11:00 - 12:20).
* Αν είχες κελιά ανά 1 ώρα, η ταινία θα έπρεπε να «σπάσει» σε δύο κελιά (11:00-12:00 και 12:00-13:00). Αυτό δημιουργεί τεράστια πολυπλοκότητα στον κώδικα!

#### 💡 Η Λύση: Το Μοντέλο του Χάρακα (Ruler / Absolute Coordinates)
Ξεχνάμε τα κελιά. 
* Κάθε κανάλι είναι ένας αυτόνομος κάθετος διάδρομος (στήλη).
* Μέσα στον διάδρομο, κάθε εκπομπή «επιπλέει» ελεύθερα με βάση:
  1. Πόσο ψηλά αρχίζει (`top` σε pixels).
  2. Πόση διάρκεια έχει (`height` σε pixels).

---

### 1.3 Εξερεύνηση στο Τερματικό (`curl`)

Πριν γράψουμε κώδικα, πρέπει να δούμε τι δεδομένα μας δίνουν.  
Ανοίγουμε το τερματικό και τρέχουμε:
```bash
curl -s "https://ext.greektv.app/epg/epg.xml" | head -n 25
```

#### Τι ανακαλύπτουμε;
1. Το αρχείο είναι τεράστιο (16+ Megabytes, 300.000+ γραμμές!).
2. Είναι γραμμένο σε **XML** και περιέχει δύο διαφορετικές οντότητες:
   * `<channel id="ert1.gr"><display-name>ΕΡΤ1</display-name></channel>`
   * `<programme start="20260902100000 +0300" stop="20260902113000 +0300" channel="ert1.gr"><title>ΣΥΝΔΕΣΕΙΣ</title><desc>Ενημερωτική εκπομπή.</desc></programme>`

---

### 1.4 Το Πρώτο Πείραμα με τη Βιβλιοθήκη `xml2js`

Η JavaScript δεν καταλαβαίνει εγγενώς XML. Χρειαζόμαστε έναν μεταφραστή. Εγκαθιστούμε το `xml2js`.

Φτιάχνουμε ένα δοκιμαστικό αρχείο και γράφουμε:
```javascript
import { parseStringPromise } from 'xml2js';

const xml_data = `<tv>...</tv>`;

async function run() {
    const result = await parseStringPromise(xml_data);
    console.log(result);
}
```

#### 💥 Το 1ο Bug / Τοίχος: Το φαινόμενο `[Object]`
Όταν τρέξαμε `console.log(result)`, το Node.js τύπωσε:
`{ tv: { channel: [ [Object] ], programme: [ [Object] ] } }`

**Γιατί συνέβη;**  
Το Node.js κρύβει τα βαθιά αντικείμενα για να μην γεμίσει η οθόνη.  
**Η Διόρθωση:** Χρησιμοποιούμε `JSON.stringify(result, null, 2)`:
* `result`: Το αντικείμενο.
* `null`: Δεν φιλτράρουμε κανένα κλειδί.
* `2`: Αφήνει 2 κενά εσοχής σε κάθε επίπεδο, κάνοντας το JSON πανέμορφο και ευανάγνωστο!

#### 💡 Κρίσιμη Διάκριση: `result` (Ζωντανό Object) vs `JSON.stringify` (Νεκρό Κείμενο)
* **`result`:** Είναι ένα **ζωντανό JavaScript Object στη μνήμη**. Πάνω σε αυτό κάνουμε loops, εξαγωγή δεδομένων και υπολογισμούς.
* **`JSON.stringify(result)`:** Είναι ένα **νεκρό κείμενο (string)**, μια «φωτογραφία» του αντικειμένου αποκλειστικά για εκτύπωση στην οθόνη ή αποθήκευση στο δίσκο.

---

### 1.5 Ο Χρυσός Κανόνας Πλοήγησης (Objects vs Arrays)

Κοιτάζοντας το αποτέλεσμα του `xml2js`, είδαμε περίεργα σύμβολα: `$` και πίνακες `[]`.

```text
┌─────────────────────────────────────────────────────────────┐
│  ΚΑΝΟΝΑΣ ΕΠΙΒΙΩΣΗΣ JAVASCRIPT:                              │
│  { } = OBJECT (Άγκιστρα) ──► Μπαίνεις με ΤΕΛΕΙΑ:  .key      │
│  [ ] = ARRAY (Αγκύλες)   ──► Μπαίνεις με ΔΕΙΚΤΗ:  [0], [1]  │
└─────────────────────────────────────────────────────────────┘
```

* **Γιατί `ch.$.id`; (Attributes):**  
  Τα στοιχεία που κάθονται *μέσα στην πρώτη ετικέτα* (`<channel id="...">`) λέγονται Attributes. Η βιβλιοθήκη `xml2js` τα μαζεύει μέσα σε ένα Object με όνομα `$`. Επειδή το `$` είναι Object, μπαίνουμε με τελεία: `ch.$.id`.
* **Γιατί `ch['display-name'][0]`; (Child Tags):**  
  Το `<display-name>` είναι εσωτερική ετικέτα. Η `xml2js` το βάζει σε Array `[]` γιατί στο XML μπορεί να υπήρχαν πολλά ονόματα σε διάφορες γλώσσες. Το `[0]` παίρνει το πρώτο στοιχείο.

---

### 1.6 Το "Σκάψιμο" των Καναλιών (Channel Transformation)

Δεν θέλουμε στο Frontend να γράφουμε `ch['display-name'][0]`. Θέλουμε καθαρό `{ id: "ert1", name: "ΕΡΤ1" }`.

```javascript
// 1. Παίρνουμε την ακατέργαστη λίστα καναλιών
const rawChannels = result.tv.channel || [];

// 2. Φτιάχνουμε νέο άδειο καλάθι
const cleanChannels = [];

// 3. Σκάβουμε τον παλιό πίνακα και πετάμε τα καθαρά στο καινούργιο
for (const ch of rawChannels) {
    cleanChannels.push({
        id: ch.$.id,
        name: ch['display-name'][0]
    });
}
```

---

### 1.7 Το "Σκάψιμο" των Προγραμμάτων (Programme Transformation)

Αφού καθαρίσαμε τα κανάλια, κοιτάμε την ακατέργαστη μορφή κάθε εκπομπής (`<programme>`):
```javascript
{
  "$": {
    "start": "20260902100000 +0300",
    "stop": "20260902113000 +0300",
    "channel": "ert1.gr"
  },
  "title": [ "ΣΥΝΔΕΣΕΙΣ" ],
  "desc": [ "Ενημερωτική εκπομπή." ]
}
```

#### Πώς διαβάζουμε κάθε πεδίο:
1. `pr.$.start`: Ώρα έναρξης (από το Object `$`).
2. `pr.$.stop`: Ώρα λήξης (από το Object `$`).
3. `pr.$.channel`: Το όνομα του καναλιού (από το Object `$`).
4. `pr['title'][0]`: Ο τίτλος της εκπομπής (από τον πίνακα `title`).
5. `pr['desc'][0]`: Η περιγραφή της εκπομπής (από τον πίνακα `desc`).

#### ⚠️ Προσοχή στα Typo λάθη:
Όταν γράφουμε `tilte` αντί για `title`, η JavaScript δεν θα χτυπήσει απαραίτητα σφάλμα εκείνη τη στιγμή, αλλά αργότερα το Frontend δεν θα βρίσκει τον τίτλο και η οθόνη θα είναι άδεια!

---

### 1.8 Γιατί 2 Ξεχωριστά Loops και όχι 1 Ενιαίο;

* Στο XML έχουμε **30 Κανάλια** και **5.000 Εκπομπές**.
* Είναι δύο εντελώς διαφορετικές λίστες (Arrays).
* Το πρώτο loop τρέχει **30 φορές** για να μαζέψει τα κανάλια.
* Το δεύτερο loop τρέχει **5.000 φορές** για να μαζέψει τις εκπομπές.
* Είναι ο πιο σωστός, καθαρός και ταχύτατος τρόπος.

---

### 1.9 Το Πρόβλημα του Χρόνου, το `Invalid Date` & Το Ψηφιακό Ψαλίδι (`.slice`)

Στο XML, η ώρα έρχεται έτσι: `"20260902100000 +0300"`.  
Αν γράψεις `new Date("20260902100000 +0300")`, η JavaScript πετάει σφάλμα: **`Invalid Date`**!

Η JavaScript απαιτεί το διεθνές πρότυπο **ISO 8601**: `"2026-09-02T10:00:00+03:00"`.

#### ✂️ Η Βοηθητική Συνάρτηση `fixedDate`:
Χρησιμοποιούμε το ψηφιακό ψαλίδι `.slice(από, μέχρι)` για να κόψουμε τους αριθμούς:

```javascript
function fixedDate(dateString) {
    const year   = dateString.slice(0, 4);   // "2026"
    const month  = dateString.slice(4, 6);   // "09"
    const day    = dateString.slice(6, 8);   // "02"
    const hour   = dateString.slice(8, 10);  // "10"
    const minute = dateString.slice(10, 12); // "00"
    const second = dateString.slice(12, 14); // "00"
    
    // Ενώνουμε με Template Literals
    return `${year}-${month}-${day}T${hour}:${minute}:${second}+03:00`;
}
```

---

### 1.10 Καθαρισμός Domain Καναλιού & Μοναδικό ID Εκπομπής

1. **Καθαρισμός Domain Καναλιού:**  
   Το κανάλι ερχόταν ως `"ert1.gr"`. Με την εντολή:  
   `pr.$.channel.toLowerCase().split('.')[0]`  
   παίρνουμε το καθαρό `"ert1"`.
2. **Μοναδικό ID (Αριθμός Ταυτότητας):**  
   Αν δύο εκπομπές σε διαφορετικά κανάλια παίζουν την ίδια ώρα (π.χ. 10:00 - 11:30), πρέπει να έχουν διαφορετική ταυτότητα. Συνδυάζουμε την ώρα έναρξης με το κανάλι:  
   `id: `${fixedDate(pr.$.start)}_${pr.$.channel}``

---

### 1.11 Ο Τελικός Κώδικας του `parser_2.js` (Έτοιμο Module)

Μετατρέψαμε το script σε επαγγελματικό **Module**:  
* Παίρνει ως παράμετρο το `xmlString`.
* Επιστρέφει (`return`) ένα ενιαίο αντικείμενο `{ channels, events }`.
* Έχει τη λέξη **`export`** μπροστά για να μπορεί να το καλέσει ο server.

```javascript
import { parseStringPromise } from 'xml2js';

function fixedDate(dateString) {
    const year   = dateString.slice(0, 4);
    const month  = dateString.slice(4, 6);
    const day    = dateString.slice(6, 8);
    const hour   = dateString.slice(8, 10);
    const minute = dateString.slice(10, 12);
    const second = dateString.slice(12, 14);
    return `${year}-${month}-${day}T${hour}:${minute}:${second}+03:00`;
}

export async function parseXmltv(xmlString) {
    const result = await parseStringPromise(xmlString);

    const rawChannels = result.tv.channel || [];
    const rawProgrammes = result.tv.programme || [];
    
    const cleanChannels = [];
    const cleanProgrammes = [];

    // Loop 1: Κανάλια
    for (const ch of rawChannels) {
        cleanChannels.push({
            id: ch.$.id,
            name: ch['display-name'][0]
        });
    }

    // Loop 2: Προγράμματα
    for (const pr of rawProgrammes) {
        cleanProgrammes.push({
            id: `${fixedDate(pr.$.start)}_${pr.$.channel}`,
            start: fixedDate(pr.$.start),
            stop: fixedDate(pr.$.stop),
            channel: pr.$.channel.toLowerCase().split('.')[0],
            title: pr['title'] ? pr['title'][0] : '',
            desc: pr['desc'] ? pr['desc'][0] : ''
        });
    }

    return {
        channels: cleanChannels,
        events: cleanProgrammes
    };
}
```

---

### 1.12 Επαγγελματικό Στήσιμο Git & `.gitignore`

Αρχικοποιήσαμε το Git στη ρίζα του project:
1. Δημιουργήσαμε το `.gitignore` ώστε να **αγνοεί** τα `node_modules/`, το `cache*.json` και το `epg.xml` (16MB).
2. Ρυθμίσαμε τα στοιχεία συγγραφέα (`git config --global user.name` και `user.email`).
3. Κάναμε το 1ο ιστορικό Commit:
   ```bash
   git commit -m "feat: complete chapter 1 - xml parser and date normalizer"
   ```

---

```text
===================================================================================
                       🎉 ΤΕΛΟΣ ΚΕΦΑΛΑΙΟΥ 1: ΑΠΟΣΤΟΛΗ ΕΞΕΤΕΛΕΣΘΗ!
===================================================================================
```

